# Cloud Functions runtime env
# In Firebase/GCP production, GCLOUD_PROJECT is usually injected automatically.
# Set these for local emulators or explicit overrides.
VERTEX_PROJECT_ID=your-project-id
VERTEX_LOCATION=us-central1
